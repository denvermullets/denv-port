<div class="flex flex-row gap-4 mt-8">
  <% links.each_with_index do |link, index| %>
    <a href="<%= link[:url] %>" target="_blank" rel="noopener noreferrer" class="btn btn-primary" data-visit="Click Link">
      <%= link[:title] %>
    </a>
  <% end %>
</div>